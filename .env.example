# Local development only
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Private controller access
ADMIN_KEY=change-this-to-a-long-random-secret

# Optional TURN server for stricter networks/NATs.
# Comma-separate multiple TURN URLs if needed.
TURN_URL=
TURN_USERNAME=
TURN_CREDENTIAL=
